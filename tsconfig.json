{
  // Nuxt 4 project-references layout (generated by `nuxt prepare` at the engine
  // root). Each context — app code, node config files, shared code — typechecks
  // under its own generated tsconfig; `npm run typecheck` (nuxt typecheck →
  // vue-tsc -b) builds all references.
  "files": [],
  "references": [
    { "path": "./.nuxt/tsconfig.app.json" },
    { "path": "./.nuxt/tsconfig.node.json" },
    { "path": "./.nuxt/tsconfig.shared.json" }
  ]
}
