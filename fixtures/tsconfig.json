{
  // Fixtures dev-app solution tsconfig: same canonical baseline, references
  // generated by `nuxt prepare fixtures`. Typechecking THIS root checks the
  // engine's code as consumed through the layer — the same shape every game
  // app gets.
  "extends": "../tsconfig.base.json",
  "files": [],
  "references": [
    { "path": "./.nuxt/tsconfig.app.json" },
    { "path": "./.nuxt/tsconfig.node.json" },
    { "path": "./.nuxt/tsconfig.shared.json" }
  ]
}
